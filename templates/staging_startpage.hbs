<div>
    <div>
        <h1>{{ title }}</h1>
        <p>{{{ description }}} </p>
        <p class="well">Total no of questions: <b>{{ totalQuestions }}</b> | Total score: <b>{{ totalScore }}</b> | Retake: <b>{{ retakeLimit }}</b></p>
    </div>
    <div class="text-center">
        <button class="btn btn-primary" data-action="start">Start</button>
    </div>
</div>